# generated by configure / remove this line to disable regeneration
prefix="/home/pbeeler/tmp/arm-linux-androideabi-5.1"
exec_prefix="${prefix}"
bindir="${exec_prefix}/bin"
libdir="/home/pbeeler/sm-tc/isl/isl-0.14.x/.libs"
datarootdir="${prefix}/share"
datadir="${datarootdir}"
sysconfdir="${prefix}/etc"
includedir="/home/pbeeler/sm-tc/isl/isl-0.14.x/./include"
package="isl"
suffix=""

for option; do case "$option" in --list-all|--name) echo  "isl"
;; --help) pkg-config --help ; echo Buildscript Of "isl Library"
;; --modversion|--version) echo "0.14"
;; --requires) echo : ""
;; --libs) echo -L${libdir} "" "-lisl -lgmp"
       :
;; --cflags) echo -I${includedir} ""
       :
;; --variable=*) eval echo '$'`echo $option | sed -e 's/.*=//'`
;; --uninstalled) exit 0 
;; *) ;; esac done
